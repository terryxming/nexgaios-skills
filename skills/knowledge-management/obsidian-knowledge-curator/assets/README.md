# 资产

本目录用于存放 `obsidian-knowledge-curator` 的可复用资产，例如样式 token、提示词片段、静态配置或示例素材。

维护要求：

- 只提交长期维护资产。
- 不提交临时截图、报告、导出文件或用户数据。

当前可用样本：

- `okc-showcase-template.md`：Obsidian 项目母文档美化版 Markdown 样本。
- `okc-showcase-snippet.css`：与样本配套的 scoped CSS snippet 起点。
