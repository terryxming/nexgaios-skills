# 面向 Codex 的工作约束（本仓库）

> 本仓库是多 agent（Claude Code + Codex）的 skill 生产流水线。任何在此干活的 agent 都必须遵守下面的工程纪律——**通用**与**私有（skill 生产）**两层，同时生效，冲突时取更严格一方。
>
> 纪律的**共享段**（`DISCIPLINE:SHARED`）在 `CLAUDE.md` 与 `AGENTS.md` 间逐字节一致，由 `tools/check-discipline-drift.py` 校验；改一份必须同步另一份。**平台专属约定**段各自维护。

<!-- DISCIPLINE:SHARED BEGIN — 本段在 CLAUDE.md 与 AGENTS.md 间须逐字节一致，由 tools/check-discipline-drift.py 校验；改一份必须同步另一份 -->

## 一分钟速览

- **单一事实源**：自主开发 skill 在 `skills/first-party/`——本仓（公开）即开发仓即分发仓（单仓公开制，见 ADR-0008）；**skill 是发布单元**，发布 = 过 G 门禁合入 main 并打 tag；`skills/third-party/` 存第三方 skill 原样副本，不参与门禁与发布（见 ADR-0010）。工程纪律常驻本文件，其"共享段"与另一平台文件须逐字节一致。
- **全面中文化**：产出物**内容**（含 `description`、正文）用中文；**标识符/文件名/路径**用英文 kebab-case。
- **决策留痕**：难逆转的选择写进 `docs/decisions/` 并附证据来源。
- **跨设备铁律**：需延续的状态必须进仓库并 push——git 是唯一同步通道。切机器先巡检，收工 handoff，续工 resume。
- **非 Claude 平台的事实**：先查官方文档再落笔；存疑项标注待实机验证，不凭记忆断言。

## 通用工程纪律

这个文件之所以存在，是因为大语言模型在写代码时会犯一些可预测的错误。不是随机错误，而是一遍又一遍出现的同一类错误。我看过太多次，所以把它们写了下来。

这些不是建议，而是规则。遵守它们，你就能写出不需要被重写的代码。忽略它们，你就会写出看起来很厉害、但在生产环境里会出问题的代码。

## 1. 写之前先读

大语言模型写出糟糕代码的最大原因，就是在写新代码之前没有阅读现有代码库。你看到一个任务，就把它匹配到训练数据里的某种模式，然后开始生成。这几乎总是错的。

在写任何东西之前：

- 阅读你即将修改的文件。不是扫一眼，而是认真读。
- 看看项目里类似的东西是怎么做的。如果 API 路由有既定模式，就遵循那个模式。如果已经有一个工具函数完成了你需要的一半功能，就使用它。
- 检查文件顶部的 imports。它们会告诉你这个项目实际使用了哪些库。如果项目到处都用 `fetch`，就不要引入 `axios`。如果项目使用原生方法，就不要引入 `lodash`。
- 查看测试文件。测试会告诉你真正期望的行为是什么，而不是你以为它应该是什么。

这里的失败模式很明显：你生成了“正确”的代码，但它和所在代码库完全格格不入。它能运行，但看起来像另一个人写的（因为确实是另一个实体写的）。于是人类要么重写它以匹配项目风格，要么永远忍受不一致。两种都不好。

如果你不确定这个项目里某件事是怎么做的，就说出来。“我在代码库里没有看到 X 的模式，我应该沿用 Y 的做法，还是采用别的方式？”这永远比猜测更好。

## 2. 编码之前先思考

不要在还没弄清楚自己到底要做什么之前就开始写代码。这听起来显而易见，但它是最常见的失败模式。

实践中应该这样做：

**说明你的假设。** 如果用户说“添加认证”，这可能意味着 session cookie、JWT、OAuth、basic auth，或者另外五种东西。不要悄悄替用户选一种。你应该说：“我假设你想要基于 JWT 的认证，带 refresh token，并存储在 httpOnly cookie 中。如果你想要别的方式，请告诉我。”如果你猜错了，只损失 10 秒。如果你沉默地猜错了，可能会损失一个小时。

**说清取舍。** 几乎每个实现选择都有取舍。如果你要加缓存，就说：“这会用内存换速度，同时引入缓存失效这个需要考虑的问题。”用户可能会说：“其实我不想要这种复杂度。”最好在你写 200 行代码之前就知道这一点。

**如果有多种方案，简要列出来。** 不要列五种。两种，最多三种，并给出推荐。“这有两种做法。方案 A 更简单，但不处理边界情况 X。方案 B 能处理所有情况，但会增加对 Z 的依赖。除非你预计 X 真的会发生，否则我会选 A。”

**如果有困惑，就停下来。** 不要用听起来合理的代码填补困惑。当你不理解需求时生成代码，结果通常是通过粗略 review 但在关键时刻失败的代码。直接说明哪里困惑，然后提问。

## 3. 简单性

写出能解决问题的最少代码。不是你能想象出来的、理论上能解决问题的最少代码，而是此时此刻真正解决这个具体问题所需要的最少代码。

过度设计的冲动很强。要抵抗它。下面是实践中过度设计的样子：

**过早抽象。** 你需要发送一种邮件。于是你写了一个 `EmailService` 类，用策略模式支持多个 provider、模板引擎和重试策略。用户想要的是 `sendWelcomeEmail(user)`。那就写这个函数。如果他们之后需要更多东西，他们会说。

```python
# 坏例子：你写了这个
class EmailService:
    def __init__(self, provider: EmailProvider, template_engine: TemplateEngine):
        self.provider = provider
        self.template_engine = template_engine

    async def send(self, template: str, context: dict, recipient: str, **kwargs):
        rendered = self.template_engine.render(template, context)
        await self.provider.send(recipient, rendered, **kwargs)

# 好例子：你本该写这个
async def send_welcome_email(user):
    body = f"欢迎 {user.name}！你的账号已经准备好了。"
    await send_email(to=user.email, subject="欢迎", body=body)
```

**猜测式错误处理。** 你给所有东西都包上 try/catch，处理那些不可能发生的错误。你验证来自自己代码、且已经在上游验证过的输入。你给永远不会是 null 的值加 null 检查。每一行错误处理，都是别人必须阅读和理解的一行代码。只处理那些真的可能发生的错误。

**不必要的可配置性。** 你把批处理大小做成参数。你把重试次数做成可配置。你为永远不会变化的东西添加环境变量。配置不是免费的。每一个配置项，都是别人必须做出的一个决策，也是别人必须正确设置的一个值。在没有真正理由之前，先硬编码。

**无用的灵活性。** 只有一个实现的接口。只有一个子类的抽象基类。只会被一种类型实例化的泛型参数。这些东西有成本：认知负担、间接层、更多需要跳转的文件。而在第二个实现真正出现之前，它们没有任何收益。

检验简单性的方法：把你的代码给一个不熟悉项目的人看。如果他们必须问“为什么这里要这样抽象？”，而答案是“以防以后需要……”，那你就过度设计了。“以防以后需要”不是需求。它只是对未来的猜测，而对未来的猜测通常是错的。

## 4. 外科手术式修改

当你编辑现有代码时，diff 应该尽可能小。你改动的每一行，都可能引入 bug，都需要别人 review，也会永远出现在 git blame 里。

规则：

**不要碰没有被要求碰的东西。** 如果你在修复函数 A 的 bug，同时注意到函数 B 的变量名很奇怪，别管它。如果函数 C 的注释有错别字，别管它。如果 import 顺序不符合你的偏好，别管它。你的工作是修复函数 A 的 bug。

**匹配现有风格。** 如果文件使用单引号，就使用单引号。如果文件使用 `snake_case`，就使用 `snake_case`。如果文件没有分号，就不要添加分号。如果文件使用 `var`（是的，即使在 2025 年），你的新增代码也使用 `var`，除非用户要求你现代化。文件内部的一致性比你的个人偏好更重要。

**清理你自己造成的问题，而不是替别人清理。** 如果你的改动导致某个 import 不再使用，就删除那个 import。如果你的改动导致某个变量不再使用，就删除那个变量。如果你的改动导致某个函数不再使用，就删除那个函数。但只有当这是你的改动造成的时候才这样做。已有的死代码不是你的问题，除非有人要求你清理。

**不要重新格式化。** 不要对一个原本没有用 prettier 格式化的文件运行 prettier。不要把缩进从 4 个空格改成 2 个空格。不要在 imports 原本没有按字母排序时把它们重新排序。重新格式化会制造巨大的 diff，掩盖你的真实改动，并让 code review 变得痛苦。

检验方法：看你的 diff。你能否说明每一行改动都和用户要求有直接关系？如果某一行存在的理由是“我刚好在这里，就顺手……”，那就还原它。

## 5. 验证

能工作的代码，和你以为能工作的代码，中间隔着测试。你应该对这个区别保持警惕。

**修 bug 时先写测试。** 在修复任何东西之前，先写一个能复现 bug 的测试。运行它。看它失败。然后修复 bug。再运行测试。看它通过。这不是可选项，也不是 TDD 教条。它是证明你真的修复了问题、而不是只让症状消失的唯一方式。

**在改动前后都运行现有测试。** 如果测试在你的改动前通过、改动后失败，那就是你弄坏了什么。这很明显。不那么明显的是：如果测试在你的改动前就已经失败，要说出来。不要默默忽略已有失败，然后让你的改动背锅。

**不要为了写测试而写测试。** 检查构造函数是否设置了属性的测试毫无价值。检查你的验证逻辑是否真的拒绝了坏输入的测试才有价值。测试行为，而不是实现。测试有意思的情况，而不是平凡的情况。

**如果你没法写测试，说明原因。** 有时架构让测试很难写。这是有价值的信息。“我没法轻松测试这个，因为数据库调用和业务逻辑紧密耦合”是一个信号，说明某些东西可能需要重构。不要只是跳过测试然后希望一切顺利。

## 6. 目标驱动执行

每个任务在你开始写代码之前，都应该有清晰的成功标准。如果标准模糊，就把它具体化。如果你无法具体化，就提问。

把模糊任务转化为可验证的任务：

- “添加验证”变成“拒绝 email 缺失或无效的输入，返回 400，并返回说明错误原因的消息，同时为两种情况添加测试”
- “修复 bug”变成“写一个复现报告行为的测试，让测试通过，并验证现有测试仍然通过”
- “提升性能”变成“先 profile，找到瓶颈，修复那个具体问题，然后再次测量”

对于任何超过一步的事情，在执行前先说明计划：

```
Plan:
1. Add the new database column with a migration
2. Update the model to include the new field
3. Modify the API endpoint to accept and return the field
4. Add validation for the field
5. Write tests for the new behavior
6. Run full test suite to check for regressions
```

这有两个作用：它让用户能在你浪费时间实现之前发现你的方案问题；同时它也强迫你真正想清楚步骤，而不是一头扎进去边做边想。

## 7. 调试

当某个东西不能工作时，不要猜。要调查。

**阅读错误信息。** 读完整条，包括 stack trace。大语言模型有一个很糟糕的习惯：看到一个错误，就根据错误类型立刻生成一个“修复”，而不是阅读错误到底在说什么。一个 `TypeError` 可能意味着一百种不同的事情。具体消息和 stack trace 会告诉你是哪一种。

**先复现。** 在你修改任何东西之前，确保你能复现问题。如果你不能复现它，就无法验证你的修复。“我觉得这应该能修好”不是调试，而是在赌博。

**一次只改一件事。** 如果你改了三件事，bug 消失了，你不知道是哪一件修好了它。你也不知道另外两件有没有引入新的 bug。改一件。测试。再改下一件。再测试。

**不要在不理解根因的情况下添加 workaround。** 如果某个值意外地是 null，不要只是加一个 null 检查然后继续。弄清楚它为什么是 null。null 检查也许能防止崩溃，但底层 bug 仍然存在，并会在之后以别的形式出现。

**如果你卡住了，就说出来。** “我试过 X 和 Y，都没有成功。这是我看到的现象。我认为问题可能是 Z，但还不确定。”这比默默随机尝试 20 轮有用得多。

## 8. 依赖

不要不加思考地添加依赖。

你添加的每个依赖，都是你无法控制的代码，并且会成为项目的永久组成部分。它需要维护、更新、做安全审计，也需要团队里的每个人理解。这个成本几乎总是比看起来更高。

在添加 package 之前：

- 你能用项目里已有的东西完成吗？如果项目已经有 `axios`，就不要添加 `node-fetch`。如果项目使用 `date-fns`，就不要添加 `moment`。
- 你能用标准库完成吗？你不需要为了 `Array.prototype.map` 使用 `lodash`。如果有 `crypto.randomUUID()`，你就不需要 `uuid`。
- 这个依赖真的还在维护吗？检查最后一次 commit 的时间。检查 issue 数量。检查维护者是否回复 issue。
- 它有多大？如果你为了格式化一个日期添加一个 500KB 的包，那通常不值得。

当你确实要添加依赖时，说明原因。“我添加 zod，是因为这个项目需要运行时 schema 验证，而现有依赖里没有能做这件事的工具”是可以的。默默往 `package.json` 里加包是不可以的。

## 9. 沟通

你如何沟通代码，和代码本身一样重要。

**说明你做了什么，以及为什么。** 不要只是丢一个代码块。“我把验证逻辑移到了单独的函数里，因为它在三个 endpoint 里重复出现。这样也能单独测试它。”这样用户不用读每一行代码，也能理解改动。

**标出担忧。** 如果你实现了用户要求的东西，但你认为这个方案有问题，要说出来。“这个能工作，但它会对列表里的每个 item 都做一次数据库调用。如果列表变大，会很慢。要我把它改成批量查询吗？”这种主动沟通可以在之后节省很多时间。

**精准说明你的不确定性。** “我不确定这个库是否支持 streaming responses”是有用的。“我觉得这应该能工作”没有用。区别在于，前者明确告诉用户需要验证什么。

**不要解释用户已经知道的东西。** 如果用户让你添加一个 REST endpoint，就不要解释什么是 REST。如果用户要求数据库索引，就不要解释索引是什么。让你的解释层级匹配用户已经展现出的知识水平。

**commit message 很重要。** 如果你在写 commit message，就写得具体一点。“Fix bug”没有用。“Fix null pointer in user lookup when email contains uppercase chars”能告诉下一个人到底发生了什么。

## 10. 常见失败模式

这些是我最常看到的模式。如果你发现自己正在做其中任何一种，就停下来重新考虑。

**厨房水槽。** 用户要求添加一个功能，你却“顺便”重构了半个代码库。不要这样。做那一件事。

**错误的抽象。** 你为一个只在一个地方存在的问题构建了一个漂亮的通用方案。重复远比错误的抽象便宜。在抽象之前，先复制粘贴两次。

**隐形决策。** 你做出了一个架构选择（数据库 schema、API 形状、认证策略），但没有把它标记为一个决策。这些选择很难逆转，用户应该知道你做了它们。

**乐观路径。** 你写的代码完美处理 happy path，却忽略或崩溃于其他所有情况。想想 API 返回 500 时会怎样。文件不存在时会怎样。用户提交空表单时会怎样。

**知识幻觉。** 你自信地使用一个不存在的 API、一个两版之前已经被移除的参数，或者一个你想象出来的库功能。如果你不是 100% 确定某个方法以这个精确签名存在，就说出来。查文档。查看项目中的实际源码。

**风格漂移。** 你用自己“偏好”的风格写代码，而不是匹配项目。OOP 代码库里写函数式模式。函数式代码库里写类。JavaScript 项目里写 TypeScript 风格。匹配代码库，而不是你的偏好。

**失控重构。** 你开始修一个东西。它牵扯到另一个东西。另一个又牵扯到下一个。二十分钟后，你改了 15 个文件，却不确定自己最初到底要做什么。如果一个修复开始连锁扩散，就停下来。告诉用户发生了什么。在继续之前先获得认可。

---

当这些准则能带来更少的无必要 diff、更少因过度复杂而导致的重写，并且澄清问题发生在实现之前而不是犯错之后时，它们就是有效的。

## 私有工程纪律（skill 生产）

上面十条是通用纪律，这一层是它们在本仓库——一个多 agent 的 skill 生产流水线——的落地，并补充 skill 特有的规则。两层同时生效，冲突时取更严格的一方。

编号约定：通用条款用数字（第 1–10 条），私有条款用字母（A–H，独立命名空间、不与数字混淆）；子条款与关卡一律以"父号·数字"引用，如 A4·3、C1·5、G·2。

### A. 每次行动都适用的行为闸门

**A1 · 查证优先，不确定就停。** 遇到非 Claude 平台的信息（Codex 的路径、字段、机制），或任何你自己拿不准的事，先查证再落笔。分流很简单：外部事实（平台、API、库、版本）联网查官方文档；项目内约定不清楚，就读仓库或问用户——项目内部的事联网查不到。查证之后，显式给出"结论加证据来源"，或"建议加理由"。难逆转、架构级的选择交用户拍板后再动；低风险且可自证的事实，附上证据就可以自行推进。如果查不到、仍然不确定，就直说"这里我不确定，不知道该如何进行"——这比擅自做一个可能错误的判断好得多。禁止背着用户悄悄决定。

**A2 · 任何写操作前先声明。** 改代码、创建、覆盖、删除、移动文件，任何落盘动作之前，先说清四件事：要做什么、怎么做、影响面波及哪些文件和流程、对项目的价值。按变更集声明一次就够，不必每个文件一次；多步任务的计划格式见通用纪律第 6 条。价值不言自明时可以省略，有实质权衡时必须写。

**A3 · 命名规范。** 标识符、文件名、路径用英文 kebab-case；内容、标题、description 用中文——"全面中文化"指内容，不指路径。用软件工程通用词，语义清晰无歧义，不用生僻或自造词。skill 的 `name` 受规范强制：只允许小写字母、数字和连字符，不超过 64 字符，且必须等于父目录名（见 E）。命名惯例拿不准时，先查证既定惯例，再向用户说明建议与依据。

**A4 · 防模型幻觉·七条。** 这是通用纪律第 10 条"知识幻觉"的可执行化，不重复它：

1. **引用即定位**：对代码、配置、API 的任何断言，必须能指向确切位置——`file:line` 或官方文档 URL。指不出来的，标注"待验证推断"，不得用确定语气陈述。
2. **工具优先于记忆**：凡是能用工具核实的（读文件、grep、跑命令、联网），一律核实，不凭记忆断言——尤其是"文件是否存在、函数签名、配置默认值"这三类重灾区。
3. **三态标注，含自评**：关键论断显式归入"已证实（附据）、推断（附依据）、未知"三态之一，禁止把推断伪装成事实。这条同等约束你自己的设计与决策——纸面定稿不等于已验证：凡是没实跑、没实测的方案，起草时就主动标「试行待验证」并在存疑段留一条，不等用户提醒。
4. **时效与版本核对**：涉及库、平台、模型能力，先读项目实际版本（lockfile、manifest），再对官方最新文档，警惕训练数据过时。记忆里的平台事实只是写入时的快照，引用前必须重新核验对应文件或 flag 仍然存在。
5. **不伪造工具产物**：绝不虚构命令输出、日志、测试结果、URL、行号。没有真正执行或读取，就明说。
6. **跨 agent 幻觉**：Claude 不臆断 Codex 的机制，反之亦然。跨平台断言一律走"联网查证加用户确认"（联动 A1）。
7. **失败诚实**：命令失败、测试红、步骤跳过，如实报原始输出，不粉饰、不假装成功。

**A5 · 门禁二值化，不留"不阻断的 warning"。** 每项检查只有两种归宿：能机械判定的，做成硬门禁——红或绿，失败即阻断，进 lint 和 CI；机器判不了但重要的，进桶三人在环——遇到即停、给出修复方案、等用户确认、再执行，同样是阻断式，不是被动提示。既判不了又不值得停的，就不要检查。禁止"warning 但放行"的中间态：不阻断的告警会被无视，日后酿成大问题，门禁只有失败或通过两态。CI 只跑二值硬门禁；桶三只在 agent 会话时生效——CI 没法"等确认"。详见 ADR-0004。

### B. 单一事实源与 MECE

同一条规则、事实、契约，只允许唯一逻辑事实源；信息组织遵循 MECE（互斥、穷尽）；禁止可避免的重复——复用时以引用或链接指向权威源，而不是复制一份。

有一个平台强制的例外：本工程纪律必须同时常驻 `CLAUDE.md` 与 `AGENTS.md`，因为 AGENTS.md 没有 import 机制、两个平台各读各的（已证实，见 ADR-0002）。所以这两份文件的共享段（通用加私有纪律）必须逐字节一致，由 `tools/check-discipline-drift.py` 做漂移校验（CI 门禁）；平台差异段各自维护，不参与比对。改了一份的共享段，必须同步另一份，校验须绿。

### C. 跨设备协作与开发巡检

你会在家用机与公司机之间接力开发。git/GitHub 是唯一同步通道，需要延续的状态必须进仓库并 push。

- **C1 · 会话起点巡检**：切换机器后、动手前，先跑开发巡检并显式报结果，同一会话内不重复。固定清单：
  1. git 是否 clean、是否落后远端，落后先 pull；
  2. 当前分支；
  3. 工具链版本（git、python、node）；
  4. 本机 Codex `project_doc_max_bytes` 是否等于 131072（128 KiB）；
  5. 读根目录 `handoff.md`（唯一交接文档）。
- **C2 · 收工 handoff**：覆盖重写根目录 `handoff.md`——唯一交接文档，固定节为当前状态、下一步、未决问题、环境备忘、上次会话摘要；历史由 git 保存，不另存副本。流水线和平台层的坑与经验追加进 `docs/lessons-learned.md`，须填"已固化到哪"。然后 commit 加 push。机械兜底：CI 的 handoff 联动检查（见 ADR-0009）。
- **C3 · 续工 resume**：先 pull，读 `handoff.md` 与 `docs/decisions/`，再动手。
- **C4 · 决策留痕**：难逆转的选择进 `docs/decisions/` 并附证据来源，避免另一台机器重复调研或凭记忆瞎猜。
- **C5 · 一 skill 一 worktree（多 agent 并行的工作区隔离）**：开发某个 `skills/first-party/<skill>` 时，为它起专属 git worktree + 分支，在独立目录开发，**不在主工作区来回切分支**。
  - 建：`git worktree add <主仓同级路径>-<skill> -b <skill>-skill main`（独立目录、分支基于 main）。发布：门禁绿 → merge main + tag → `git worktree remove <目录>`。
  - 主克隆根目录停在 main / 作集成区，不当单 skill 的开发区、不来回切分支。
  - 理由：本仓库多 agent（Claude+Codex）协作，共享单一工作区会被并行会话切换 HEAD——导致 commit 落错分支、工作区文件被切走"消失"（已发生并返工）。worktree 给每个 skill/agent 独立 HEAD 与物理目录：文件恒可见、分支不被切走、并行零踩踏，共享同一 `.git`。
  - 联动：commit/reset/add 前先 `git branch --show-current` 核对当前分支；commit 用精确路径、不 `git add -A`。

### D. 通用纪律 → skill 场景映射

通用十条落到 skill 生产上，各自长这样：

| 通用纪律 | 在本仓库的形态 |
|---|---|
| 1. 写之前先读 | 新建 skill 前先读同类 skill 与官方 skill-creator 指引；改前先读其 `SKILL.md`、`evals/` 与 git tag 版本历史。 |
| 2. 编码前思考 | 先说清触发边界（何时触发 / 何时不）、平台差异、是否真需新建。 |
| 3. 简单性 | 一个 skill 只解决一类任务；`SKILL.md` 正文短小，细节拆 `references/` 按需加载。 |
| 4. 外科手术式修改 | 一次只动一个 skill 目录。 |
| 5. 验证 | 改 `SKILL.md` 前先把问题写成 eval 用例，先挂后过（见 F）。 |
| 6. 目标驱动 | "做个 X skill"先具体化为触发正/负例 + 执行 rubric。 |
| 7. 调试 | 不触发/误触发先复现（eval），再定位是 `description` 还是正文，一次改一处。 |
| 8. 依赖 | `scripts/` 能用标准库不引三方；`compatibility` 如实声明。 |
| 9. 沟通 | 难逆转决策进 `docs/decisions/` 附证据。 |
| 10. 失败模式 | 尤其警惕知识幻觉（见 A4）。 |

### E. skill 特有约束

skill 层的检查按 A5 的机制分三桶：桶一委托官方工具，桶二自建硬门禁，桶三人在环。三桶与 G 发布门禁只管 `skills/first-party/`（自主开发的发布单元）；`skills/third-party/` 是第三方 skill 的原样收藏，不参与任何门禁与发布，入库须有再分发许可并在 `skills/third-party/sources.md` 留溯源（见 ADR-0010）。

**桶一 · 委托官方 `skills-ref validate`。** 校验 frontmatter 与命名，二值 pass/fail，不自己重写（见 B）；Python 3.11 起，`pip install skills-ref`。它管的范围：`name` 不超过 64 字符、只含小写字母数字连字符、不以连字符起止、无连续连字符、必须等于父目录名；`description` 非空且不超过 1024；`compatibility` 是字符串且不超过 500；frontmatter 无白名单外字段；结构完整——路径、目录、`SKILL.md` 存在且可解析。

**桶二 · 自建硬门禁**，管 skills-ref 不覆盖、但可以脚本化的检查，进 lint 和 CI：

1. **平台可移植**：skill 源不写死机器路径或平台假设；平台差异（如 Codex 的 `agents/openai.yaml` 这类 sidecar）与 `SKILL.md` 分离。
2. **中文化兵底**：`description` 与正文至少含中文（CJK），防纯英文产出。
3. **杂物拦截**：skill 顶层禁止 README.md 等与 `SKILL.md` 职责重复的文档；`dev-log.md` 与 `CHANGELOG.md` 属域内开发文件，随 skill 入库并一同分发——源头干净，而非出口净化（见 ADR-0008）。
4. **版本存在**：`SKILL.md` 须有 `metadata.version`（G·3 的前置）。

**桶三 · 人在环质量关**，管语义上机器判不了的检查，遇到即停、给提案、等确认（见 A5）：

1. **`description` 质量**：用第三人称讲清三件事——这个 skill 是干嘛的、何时用、何时不用。skills-ref 只查非空，质量靠人把关。
2. **中文化质量**：中文为主；通用与专业术语保留英文并加中文注释，形如"TDD（测试驱动开发）"，不生硬直译。
3. **正文长度**：`SKILL.md` 正文过长时拆 `references/` 按需加载。spec 建议 500 行以内，非硬性。

### F. 评测即回归

评测与创作引擎委托官方 skill-creator，不自建 runner（见 ADR-0005）。触发评测（正例负例、多次采样得出触发率）与执行评测（with-skill 对 no-skill baseline 的子代理对照、grader 打分、benchmark 聚合）都用它的机器；仓库只定阈值、消费结果。

- **用例人在环**：评测用例（触发正负例、执行场景）在运行前必须给用户过目确认——是否贴近真实场景由人判定，不许闷头自测。
- **用例格式**采用 skill-creator 约定：`skills/first-party/<name>/evals/evals.json` 加触发查询 JSON，复用它的 viewer、aggregate、run_loop，不自造格式。
- **失败即用例**：线上每个失败案例先回填成一条 eval 用例，再改源码；改完跑全量回归，通过才允许重新发布。
- 用例入库 `skills/first-party/<name>/evals/`，运行产物不入库。

### G. 发布门禁

允许发布或更新一个 skill，必须依次通过五关，任何一关不过都不予发布：

1. `skills-ref validate` 加自建 lint 全绿；
2. 该 skill 的触发与执行评测达标；
3. 版本按 skill 独立 bump（`metadata.version`）；
4. 合入 main 并打 tag `skill-name/vX.Y.Z`（main = 可安装态）；
5. 涉及难逆转决策的，`docs/decisions/` 有记录。

- 版本的机械参照是 git tag `skill-name/vX.Y.Z` 加 `metadata.version`；`CHANGELOG.md` 是域内给人读的版本叙事，随 skill 入库并一同分发（见 ADR-0006/0008）。
- **main = 可安装态**：`skills/first-party/<name>/` 的改动在分支上进行，全部门禁通过后 merge 进 main 并打 tag——外部用户从 main 装到的永远是已发布版。流水线自身文件（`tools/`、`docs/`、`handoff.md`、纪律双份）可在 main 直接迭代。
- 五关的落点（见 ADR-0004/0005/0008）：G·1 与 G·3 进 CI 常驻，G·3 以 git tag 作参照；G·4 就是发布动作本身——merge 加 tag，人在环；G·2 由 agent 在发布时驱动——用 skill-creator 跑评测、留痕、达标才发，阈值是全局底线（负例误触发为零、优于 baseline）加每 skill 可调；G·5 发布时人在环——停下来等确认。

### H. 外部 skill 迁入（收编与 fork）

自己在别处创建的 skill 要转移进本仓库管理（收编），走四步：

1. **单一源转移**：拷入 `skills/first-party/<name>/` 起，本仓库即该 skill 的唯一事实源；源仓库同步退役——删除或标注"已迁至本仓库"，防双源漂移（见 B）。先确认迁入落地（lint 绿加 push），再执行退役。
2. **门禁整改**：以 `tools/lint.py` 全绿为准——name 等于目录名、清掉 README.md 等重复文档、含中文、无机器路径。`dev-log.md` 与 `CHANGELOG.md` 属域内开发文件，随迁保留。`description` 三要素与中文化质量走桶三人在环。
3. **版本接续**：保留原 `metadata.version`，没有就从 `0.1.0` 起；迁入时打基线 tag `skill-name/vX.Y.Z`。
4. **评测补课**：evals 缺失或异构不阻入库，但按 F 补齐并达标之前不得发布（G·2）。

另一条路是**改造第三方 skill（fork）**：从 `skills/third-party/` 复制到 `skills/first-party/<name>/`，此后按本仓规则迭代发布；上游继续存在、不退役，副本须记 upstream 出处，license 义务随行，版本从 fork 点起。两条路的分界是源头归属——自己的走收编（源退役），别人的走 fork（源共存）。

无论收编还是 fork，进入 `skills/first-party/` 后的迭代与发布同本仓库原生 skill，没有特殊通道。

<!-- DISCIPLINE:SHARED END -->

<!-- DISCIPLINE:PLATFORM BEGIN — 本段仅属当前文件，由当值平台自行维护，不参与漂移校验 -->

## 平台专属约定（Codex）

> 本段仅属 `AGENTS.md`，由 Codex 维护，不参与与 `CLAUDE.md` 的漂移校验（见私有纪律 B）。
> 以下由 Claude 依官方文档查证初始化（见 ADR-0001 / ADR-0002），**待 Codex 当值时复核接管**。

- skill 读取路径：仓库 `.agents/skills`（从 cwd 向上扫到仓库根）、用户 `~/.agents/skills`、管理员 `/etc/codex/skills`；支持 symlink。（2026-07-03 实机证据：本机内置 skill 在 `~/.codex/skills/.system/`，官方 skill-creator 亦称无 `CODEX_HOME` 时回落 `~/.codex/skills`——用户级路径倾向 `~/.codex/skills`，待 Codex 当值复核，见 ADR-0001 存疑项）
- 平台特有配置走 sidecar `agents/openai.yaml`（`display_name`、`allow_implicit_invocation`、MCP `tools` 依赖），不污染 `SKILL.md`。
- `AGENTS.md` 合并读取上限 `project_doc_max_bytes` 默认 32 KiB；本仓库要求各机器设为 `131072`（128 KiB）。巡检项见私有纪律 C1。

<!-- DISCIPLINE:PLATFORM END -->
